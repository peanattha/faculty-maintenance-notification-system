<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Home</title>
    <!-- BOOTSTRAP CDN -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>

<body class="container" style="margin-top: 50px;">
    <div class="d-flex justify-content-between align-items-center">
        <h1>Hello User : <%= data[0].name %></h1>
        <a href="/logout" class="btn btn-danger">Logout</a>
    </div>
    <div class="card">
        <div class="card-header">
            <ul class="nav nav-pills w-100">
                <li class="nav-link">รายการแจ้งซ่อมของฉัน</li>
                <li class="nav-link ml-auto">
                    <a href="/repair/add" class="nav-link active">เพิ่มรายการเเจ้งซ่อม</a>
                </li>
            </ul>
        </div>
        <div class="card-body">
            <% if(data.length) { %>
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">ลำดับ</th>
                            <th scope="col">repair id</th>
                            <th scope="col">Name</th>
                            <th scope="col">status</th>
                            <th scope="col">details</th>
                            <th width="200px">แก้ใข/ลบ</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(let i=0; i < data.length; i++) { %>
                            <tr>
                                <th scope="row">
                                    <%= i+1 %>
                                </th>
                                <td>
                                    <%= data[i].repair_id %>
                                </td>
                                <td>
                                    <%= data[i].name %>
                                </td>
                                <td>
                                    <%= data[i].status %>
                                </td>
                                <td>
                                    <%= data[i].details %>
                                </td>
                                <td>
                                    <a href="../books/edit/<%= data[i].id %>" class="btn btn-success edit">Edit</a>
                                    <a href="../books/delete/<%= data[i].id %>" onclick="return alert('Are you sure?')"
                                        class="btn btn-danger delete">Delete</a>
                                </td>
                            </tr>
                            <% } %>
                    </tbody>
                </table>
                <% } %>

                    <!-- if result is empty -->
                    <% if(!data.length) { %>
                        <p class="text-center">ยังไม่มีรายการเเจ้งซ่อม!</p>
                        <% } %>
        </div>
    </div>

</body>

</html>